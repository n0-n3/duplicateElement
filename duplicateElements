let arr = [5, 1, 4, 6, 7, 3, 5, 7, 3];
let uniqueElements = {};
for (let i = 0; i < arr.length; i++) {
  if (arr[i] in uniqueElements) {
    console.log(arr[i]);
  } else {
    uniqueElements[arr[i]] = true;
  }
}
